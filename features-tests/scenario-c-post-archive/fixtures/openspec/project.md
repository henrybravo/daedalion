# Security Agent Platform

AI-powered security operations platform built on Microsoft Agent Framework SDK.

## Conventions

- Use snake_case for Python module and function names
- All tools must support mock mode via environment variable
- Every tool registers with the agent via the framework decorator pattern
